apiVersion: v1
kind: Secret
metadata:
  name: post-service-secrets
type: Opaque
stringData:
  # Azurite example (cluster DNS). Replace with real Azure connection string in production.
  AZURE_CONNECTION_STRING: "DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=REPLACE_ME;BlobEndpoint=http://azurite:10000/devstoreaccount1"
